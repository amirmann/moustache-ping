# Moustache Ping v1.0.13

## Fixes

- **Network Info** — Wi‑Fi SSID/BSSID show again
- **Android 13+** — requests `NEARBY_WIFI_DEVICES` with `neverForLocation` first (no location-style disclosure for that permission)
- **Older Android / fallback** — if SSID is still blank, asks for location when in use only while refreshing Network Info (for Wi‑Fi name, not tracking); never background location

## Download

- `moustache-ping-v1.0.13-arm64.apk`
