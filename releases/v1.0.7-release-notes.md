# Moustache Ping v1.0.7

## Changes

### Subnet Scan — Diff hostnames
- Added and removed hosts in the scan diff now show hostname when reverse DNS resolved it
- Hostname is shown as the primary label, with the IP underneath (same style as the host list)
- Hostnames that resolve after the scan finishes still update the diff live
