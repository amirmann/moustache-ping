# Moustache Ping v1.0.11

## Fixes

- **Copy MAC fully removed** from long-press host menu (hostname + IP only)
- **Ping** — clear (X) button on the target input
- **Scan** — clear (X) on CIDR input; WiFi detect always re-fills the current network after clearing
- **Settings** — version always shown as `1.0.11 (12)` (no longer depends on PackageInfo)

## Download

- `moustache-ping-v1.0.11-arm64.apk`

Uninstall any older build first if the old long-press menu still appears.
