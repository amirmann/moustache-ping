# Moustache Ping v1.0.12

## Fixes

- **Settings** — version shows marketing name only (`1.0.12`), not the Android build number
- **Scan** — CIDR field is full-width; Scan/Stop sits on the row below so the network text is never clipped
- **Permissions** — no location permission request or declaration; WiFi IP/CIDR still via native network APIs (SSID may be unavailable on some Android devices)

## Download

- `moustache-ping-v1.0.12-arm64.apk`
